# TODO

- Split the build output from the tracked GitHub Pages deploy directory when the deployment workflow is ready to build from source.
- Add richer blog media helpers if posts need captions, galleries, or image sizing controls beyond standard Markdown images.
